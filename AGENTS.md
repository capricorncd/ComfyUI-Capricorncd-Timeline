# 项目开发规则

## Rule：每次写代码前先执行规则检查

- 每次任务开始写代码前（包括后续追加修改），先读取当前适用的 `AGENTS.md`，包括上级目录与目标文件所在子目录的规则；不得先改代码再补看规则。
- 按本次改动范围读取规则引用的规范。涉及 UI 时，先阅读 `docs/ui-guidelines.md` 和 `docs/ui-components.md` 的相关章节。
- 动手前核对本次实现的约束、已有组件或同类实现、验证方式，然后按规则编码。规则检查由开发者主动完成，不要求用户重复提醒或确认。
- 任务中范围或要求变化时，修改前重新核对适用规则；交付前按相关规范检查结果。

## 开发前必读

- 新增或修改 UI 前，阅读并遵循 `docs/ui-guidelines.md`（设计与验收规范），组件接口参见 `docs/ui-components.md`。
- 必须保持标题、正文、辅助信息的层级及内容左右留白；只读提示词完整展开，随弹窗内容区统一滚动；复制按钮只使用公共图标，不显示文字，并提供可访问名称和成功反馈。
- 当前任务涉及的界面按规范实现；不要为统一外观批量改动无关页面。

## Web Component 优先

- UI 优先使用原生 Web Component，统一放在 `js/components/`。编码前先查找可复用组件；已有能力优先复用，缺少通用能力时扩展已有组件，重复的完整交互再提取为共享组件。
- 组件统一负责内部布局、样式、基础交互、可访问性和状态反馈；业务页面负责数据获取、业务操作、持久化及组件外部布局，不复制组件的 HTML、CSS、事件处理或反馈逻辑。
- 通过明确的属性、方法、slot 和自定义事件通信，不从业务页面穿透 Shadow DOM 操作组件内部节点。主题沿用公共 CSS 变量。
- 提取组件时迁移本次涉及的重复调用点并删除被替代的实现，更新组件文档。不要为一次性静态结构增加空壳组件、无实际用途的参数或新依赖，也不要顺带重构无关页面。

## 按钮必须使用组件

- 新增按钮统一使用 `js/components/Button.js` 的 `<cap-button>`；下拉入口使用 `js/components/DropdownButton.js` 的 `<cap-dropdown-button>`，后者内部复用前者。
- 不在业务页面、弹窗、工具栏或编辑器模块中新增原生 `<button>`、`document.createElement("button")` 或等价写法。原生按钮仅允许作为共享组件内部的实现。
- 不新增页面专属的按钮颜色、边框、间距、悬停、选中或禁用样式。页面只负责组件外部布局；样式与状态通过组件接口统一维护。
- 特殊需求先扩展已有组件；Tab、单选组等需要独立语义时，使用或新增组合按钮组件的对应组件，不把它们降级为普通点击按钮。
- 维护旧按钮时优先迁移到组件，移除被替代的按钮样式，并检查事件委托、焦点、键盘操作、禁用及选中状态。不要为无关任务批量重写全部旧 UI。
- 图标按钮必须有可访问名称；业务代码负责动作与状态，组件负责呈现与基础交互。

组件接口与测试说明见 `docs/ui-components.md`。

## 弹窗必须使用组件

- 新增弹窗使用 `js/components/Dialog.js` 的 `<cap-dialog>`，不在业务模块新建原生 `<dialog>` 或复制遮罩、容器、标题栏、关闭和拖动实现。
- `showModal()` 用于阻塞式弹窗；`show()` 用于无背景遮罩、允许操作底层界面的浮窗。关联生成视频必须使用后者，不得把时间轴设为 inert 或暂停其键盘交互。
- 标题通过 `slot="title"` 提供；内容保留在 light DOM。组件负责每次打开居中、标题栏拖动、视口边界、最大 80vw/80vh、内部滚动、阴影、关闭按钮和 Esc；业务模块只提供内容、尺寸变量与操作。
- 弹窗按 header / body / footer 分区：确认、取消、导出、运行等全局操作放在直属的 `slot="footer"` 容器，只有 body 滚动。无全局操作时省略 footer；表单内的局部操作仍留在对应内容旁。旧弹窗使用同样的固定头尾布局。
- 组件右下角支持手动缩放。每种弹窗按内容配置 `--cap-dialog-min-width`、`--cap-dialog-min-height`（px）；最小尺寸不得突破视口最大限制。关闭按钮复用公共 Lucide X SVG，不使用文本叉号。
- 使用 `closeDisabled` 或可取消的 `cancel` 事件阻止进行中的任务被用户关闭；用 `close` 事件清理音频试听等业务资源。程序主动关闭使用 `close()`。
- 旧弹窗逐步迁移期间复用组件导出的 `bindDialogDrag` 和公共阴影/遮罩变量，不新增另一套拖动或弹窗样式。
